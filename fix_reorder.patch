--- Frontend/src/utils/reorderTasks.js
+++ Frontend/src/utils/reorderTasks.js
@@ -7,6 +7,10 @@
     buckets.get(task.column_id).push(task);
   });
 
+  buckets.forEach((list) => {
+    list.sort((a, b) => (a.position ?? 0) - (b.position ?? 0));
+  });
+
   let movingTask = null;
   let sourceColumnId = null;
   buckets.forEach((list, columnId) => {
